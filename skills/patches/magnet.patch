--- a/hdmi_coin.gprj
+++ b/hdmi_coin.gprj
@@ -14,6 +14,7 @@
         <File path="src/game/game_core.v" type="file.verilog" enable="1"/>
         <File path="src/game/game_ctrl.v" type="file.verilog" enable="1"/>
         <File path="src/game/skill_slot.v" type="file.verilog" enable="1"/>
+        <File path="src/game/skill_magnet.v" type="file.verilog" enable="1"/>
         <File path="src/game/spawn_postprocess.v" type="file.verilog" enable="1"/>
         <File path="src/game/spawn_queue.v" type="file.verilog" enable="1"/>
         <File path="src/hdmi/svo_defines.vh" type="file.verilog" enable="1"/>
--- a/src/game/game_ctrl.v
+++ b/src/game/game_ctrl.v
@@ -15,6 +15,8 @@
 	parameter TIMER_START = 60,
 	parameter TIME_BONUS = 3,
 	parameter SKILL_CHARGE_MAX = 5,
+	parameter MAGNET_DURATION = 15,
+	parameter MAGNET_PAD = 16,
 	parameter HIGH_SCORE_START = 0
 )(
 	input clk,
@@ -96,14 +98,21 @@
 assign obj_ready = obj_has_room;
 assign game_over = state == S_OVER;
 
-skill_slot u_skill_slot (
+wire skill_start;
+wire magnet_on;
+
+skill_magnet #(
+	.DURATION(MAGNET_DURATION)
+) u_skill_magnet (
 	.clk(clk),
 	.resetn(resetn),
 	.tick(frame_tick && state == S_PLAY && !pause),
 	.restart(btn_start_rise),
 	.btn_skill(btn_skill),
 	.skill_charge(skill_charge),
-	.skill_timer(skill_timer)
+	.skill_timer(skill_timer),
+	.magnet_on(magnet_on),
+	.start_pulse(skill_start)
 );
 
 spawn_queue u_spawn_queue (
@@ -142,8 +151,11 @@
 reg hit_valid;
 reg [4:0] hit_idx;
 reg [9:0] hit_obj_x;
-wire [10:0] hit_player_l = player_x;
-wire [10:0] hit_player_r = player_x + PLAYER_WIDTH;
+wire [10:0] hit_player_l = magnet_on && player_x > MAGNET_PAD ?
+							player_x - MAGNET_PAD : player_x;
+wire [10:0] hit_player_r = magnet_on ?
+							player_x + PLAYER_WIDTH + MAGNET_PAD :
+							player_x + PLAYER_WIDTH;
 wire [10:0] hit_player_t = PLAYER_Y;
 wire [10:0] hit_player_b = PLAYER_Y + PLAYER_HEIGHT;
 
@@ -309,6 +321,9 @@
 					skill_charge <= next_charge;
 				end
 
+				if (skill_start)
+					skill_charge <= 0;
+
 				// Object falling and spawning
 				if (remove_valid) begin
 					for (i = 0; i < MAX_OBJ-1; i = i + 1) begin
--- /dev/null
+++ b/src/game/skill_magnet.v
@@ -0,0 +1,49 @@
+`timescale 1ns / 1ps
+
+module skill_magnet #(
+	parameter DURATION = 15,
+	parameter CHARGE_MAX = 5
+)(
+	input clk,
+	input resetn,
+	input tick,
+	input restart,
+	input btn_skill,
+	input [2:0] skill_charge,
+
+	output reg [7:0] skill_timer,
+	output magnet_on,
+	output start_pulse
+);
+reg [5:0] sec_cnt;
+reg btn_q;
+
+assign magnet_on = skill_timer != 0;
+assign start_pulse = btn_skill && !btn_q && skill_charge >= CHARGE_MAX && !magnet_on;
+
+always @(posedge clk) begin
+	if (!resetn) begin
+		skill_timer <= 0;
+		sec_cnt <= 0;
+		btn_q <= 0;
+	end else if (restart) begin
+		skill_timer <= 0;
+		sec_cnt <= 0;
+		btn_q <= 0;
+	end else begin
+		btn_q <= btn_skill;
+
+		if (start_pulse) begin
+			skill_timer <= DURATION;
+			sec_cnt <= 0;
+		end else if (tick && skill_timer != 0) begin
+			if (sec_cnt == 59) begin
+				sec_cnt <= 0;
+				skill_timer <= skill_timer - 1;
+			end else begin
+				sec_cnt <= sec_cnt + 1;
+			end
+		end
+	end
+end
+endmodule
